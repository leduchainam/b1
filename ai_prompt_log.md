\# Nhật ký Tương tác AI (AI Prompt Log) - PayFlow Performance Tuning

Lượt 1: Thuật ngữ SARGable và cơ chế vô hiệu hóa IndexPrompt: "Trong MySQL, nếu tôi tạo B-Tree Index cho cột created\_at, tại sao viết WHERE YEAR(created\_at) = 2026 lại khiến MySQL từ chối dùng Index và rơi vào Full Table Scan?"Tóm tắt phản hồi: SARGable viết tắt của Search Argument Able. B-Tree sắp xếp các giá trị thô có thứ tự. Khi bọc hàm YEAR(col), MySQL không thể dự đoán giá trị trả về nếu không chạy hàm trên từng dòng. Kết quả là DB Engine buộc phải duyệt toàn bộ bảng để tính toán, làm vô hiệu hóa khả năng tìm kiếm nhị phân (Binary Search).Lượt 2: Thứ tự cột trong Composite Index (Leftmost Prefix Rule)Prompt: "Khi tạo Composite Index cho 2 cột (transaction\_type, created\_at), thứ tự cột nên sắp xếp thế nào khi WHERE chứa phép '=' và phép so sánh khoảng '>= / <'?"Tóm tắt phản hồi: Theo quy tắc tiền tố ngoài cùng bên trái (Leftmost Prefix Rule), cột áp dụng phép so sánh bằng (=) phải đặt trước (transaction\_type), sau đó mới đến cột so sánh khoảng (created\_at). Nếu đặt cột khoảng đứng trước, các cột phía sau không thể tối ưu hóa tìm kiếm nhị phân được nữa.Lượt 3: Ý nghĩa các chỉ số trong cột 'type' của EXPLAINPrompt: "Giải thích thang đo hiệu năng của cột 'type' trong MySQL EXPLAIN từ tốt nhất đến tệ nhất: const, ref, range, index, ALL."Tóm tắt phản hồi:system / const: Đọc 1 dòng duy nhất qua Primary Key / Unique Key (nhanh nhất).ref: Khớp dữ liệu với một giá trị cụ thể qua Non-unique Index.range: Quét một khoảng giá trị qua Index (BETWEEN, <, >, IN).index: Full Index Scan (quét toàn bộ cây Index).ALL: Full Table Scan (quét toàn bộ đĩa dữ liệu vật lý, chậm nhất).Lượt 4: Cấu trúc cây B-Tree và Overhead của IndexPrompt: "Phân biệt Index Seek và Index Scan trên cây B-Tree. Tại sao tạo quá nhiều Index lại làm giảm hiệu năng của các lệnh INSERT, UPDATE, DELETE?"Tóm tắt phản hồi: Index Seek định vị nhanh từ gốc đến lá theo độ phức tạp $O(\\log N)$, còn Index Scan duyệt qua các node lá liên tiếp. Khi ghi dữ liệu (INSERT/UPDATE/DELETE), MySQL phải khóa và tái cân bằng đồng thời tất cả các cây B-Tree (Node Split / Page Merge), gây gia tăng I/O đĩa và làm chậm tốc độ ghi.



